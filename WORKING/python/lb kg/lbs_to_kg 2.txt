weight = int(input('enter ur weight: '))
unit = input('(L)bs or (K)g ' )


if unit.upper() == "K":
    kg = (weight * 2.20462)
    print("ur weight in pound :" + str(kg) )
    
if unit.upper() == "L":
    lb = (weight // 2.20462)
    print("ur weright in kilo: " + str(lb) )